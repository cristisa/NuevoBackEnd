{% extends 'base.html.twig' %}
  
{% block title %}Fichar entrada{% endblock %}
  
{% block body %}
    <div class="container">
        <h2 class="text-center mt-5 mb-3">Fichar entrada</h2>
        <div class="card">
            <div class="card-header">
                <a class="btn btn-outline-info float-right" href="{{ path('app_signin_index') }}"> 
                    Ver todos los fichajes
                </a>
            </div>
            <div class="card-body">
                 {{ include('signin/_form.html.twig') }}
            </div>
        </div>
    </div>
{% endblock %}